
import { StyleSheet, Text, View } from 'react-native';
import Historia from './src/components/Historia';
import Route from './src/rotas';
export default function App() {
  return (
    <Route></Route>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#75A4F0',
    alignItems: 'center',
    
  },
});
